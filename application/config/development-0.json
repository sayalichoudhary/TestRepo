/**
 * This is just a dummy file because we are using pm2 for deploying on server instance.
//Where 'production' is the 'NODE_ENV' variable value and '0' is the node process id deployed in pm2.
 * Created by API Defender on 22/10/15
 * @version 1.0
 */

{}